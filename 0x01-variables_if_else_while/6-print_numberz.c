#include <stdio.h>

/**
 * main - Prints numbers of single digit
 *
 * Return: Always (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}

